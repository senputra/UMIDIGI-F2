The library 'librpc.so' is a library for remote procedure call.
This library is pre-built by MediaTek internal.


WHAT IT DOES?
=============

implement remote procedure call


HOW IT WAS BUILT?
==================

All source/dependency modules of this module are already put in
'vendor/mediatek/libs' folder


HOW TO USE IT?
==============

It is used by libviagpsrpc.so only.