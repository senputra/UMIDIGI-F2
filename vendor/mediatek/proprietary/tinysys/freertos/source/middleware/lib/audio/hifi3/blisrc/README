The library 'libblisrc.a' is the audio resampler implementation.
These library is pre-built by MediaTek internal.


WHAT IT DOES?
=============

libblisrc.a is an audio resampler, used to transform audio data sampling rate.

HOW IT WAS BUILT?
==================

It was built in Xtensa IDE.


HOW TO USE IT?
==============

The library 'libblisrc.a' is be used by
audio driver to generate an appropriate sampling rate data.

All the source codes of this library were written by MediaTek co..
