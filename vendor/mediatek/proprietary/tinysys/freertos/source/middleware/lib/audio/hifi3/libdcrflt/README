The library 'libdcremoval.a' is the audio dc filter.
These library is pre-built by MediaTek internal.


WHAT IT DOES?
=============

libdcremoval.a is an audio dc filter, used to remove dc offset of audio data.

HOW IT WAS BUILT?
==================

It was built in Xtensa IDE.


HOW TO USE IT?
==============

The library 'libdcremoval.a' is be used by
audio driver to remove dc offset of audio data.

All the source codes of this library were written by MediaTek co..
